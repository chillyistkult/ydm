machine:
  node:
    version: 6.0.0
dependencies:
  pre:
    - sudo pip install awsebcli --ignore-installed setuptools
deployment:
  production:
    branch: master
    commands:
      - eb deploy --profile default
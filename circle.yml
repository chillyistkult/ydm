machine:
  node:
    version: 6.0.0
dependencies:
  pre:
    - sudo pip install 'awsebcli==3.7.4' --force-reinstall
deployment:
  production:
    branch: master
    commands:
      - eb deploy --profile default